networkx>=3.0
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
openpyxl>=3.1
scipy>=1.10
ipywidgets>=8.0
# osmnx is required but must be installed separately:
#   pip install osmnx
# It is omitted here because it has heavy C dependencies that
# may need platform-specific handling on some systems.
